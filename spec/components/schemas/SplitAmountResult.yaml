type: object
required:
  - splitUnit
  - currency
  - offsetAmounts
properties:
  splitUnit:
    $ref: '#/components/schemas/SplitUnit'
  currency:
    $ref: '#/components/schemas/Currency'
  offsetAmounts:
    type: array
    items:
      $ref: '#/components/schemas/OffsetAmount'