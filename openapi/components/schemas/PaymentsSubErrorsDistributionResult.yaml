type: object
allOf:
  - type: object
    required:
      - error
      - percents
    properties:
      error:
        $ref: '../schemas/SubError.yaml'
      percents:
        description: Относительное колличество ошибок в процентах
        type: number
        format: double
        minimum: 0