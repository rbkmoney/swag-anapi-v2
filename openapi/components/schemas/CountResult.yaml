type: object
allOf:
  - type: object
    required:
      - count
      - currency
    properties:
      count:
        description: 'Общее количество операций'
        type: integer
        format: int64
        minimum: 0
      currency:
        $ref: '../schemas/Currency.yaml'