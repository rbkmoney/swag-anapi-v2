description: Время жизни инвойса с момента его создания.
type: object
properties:
  years:
    type: integer
    format: int64
  months:
    type: integer
    format: int64
  days:
    type: integer
    format: int64
  hours:
    type: integer
    format: int64
  minutes:
    type: integer
    format: int64
  seconds:
    type: integer
    format: int64